filename rpt '/dwh/compare/wiis_missing_7_variables.txt';

proc printto print=rpt new;
run;

libname old '/dwh/test';
libname new '/dwh/compare';

title "OLD library members";
proc datasets lib=old memtype=data;
run;
quit;

title "NEW library members";
proc datasets lib=new memtype=data;
run;
quit;

title "OLD.WIIS contents";
proc datasets lib=old nolist;
    contents data=wiis varnum;
run;
quit;

title "NEW.WIIS contents";
proc datasets lib=new nolist;
    contents data=wiis varnum;
run;
quit;

proc printto;
run;
